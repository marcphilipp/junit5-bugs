description = ''
ext.moduleName = 'testmodule'

dependencies {
	implementation project(':projectB')
}

junitPlatform {
	enableModulePath = true

	if (!rootProject.hasProperty('noJUnitFilters')) {
		filters.packages.include = [
			'testpackage',
		]
	}
}
